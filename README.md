# Video Synchronization Project

### Getting started
1. Install mise
2. Go to the root of this repository
3. Run `mise trust` to trust this repository
4. Run `mise install` to install uv and the selected Python version
5. Run `uv sync` to install Python dependencies
6. Run `python main.py`

